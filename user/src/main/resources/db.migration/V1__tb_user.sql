CREATE TABLE tb_user.usuarios (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   nome VARCHAR(200),
   sexo CHAR,
   nascimento TIMESTAMP WITHOUT TIME ZONE,
   login VARCHAR(255),
   senha VARCHAR(255),
   CONSTRAINT pk_usuarios PRIMARY KEY (id)
);